import path from 'path';

module.exports = {
  root: path.resolve(__dirname, '../../public'),
  protocol: 'http',
  host: 'localhost',
  port: 3000,
  base: '/',
  asset: 'assets/',
  thumbnail: 'thumbnails/',
};
